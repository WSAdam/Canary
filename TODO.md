# Canary TODO

- [x] Spend guardrail no longer pages hourly on a genuine $0 at billing-cycle reset — {total:0,items:[]} with an explicit empty items array is accepted; $0 without any items structure still fails loud (deno-spend.ts)
- [x] Test suite runs on an in-memory KV (CANARY_KV_PATH) so leftover local rows from a different Deno/V8 version can't poison it with deserialize errors (_kv.ts, deno.json)
- [x] Reports 30d window loads instead of timing out — scanWindow batches KV reads rather than one round-trip per run row (runResult.ts)
- [x] A corrupt run row still truncates history at that row and surfaces its purgeable banner after the batching change (runResult.ts)
- [x] /api/reports scans monitors in parallel and skips the always-failing check lookup for relay monitors (main.ts)
- [x] Cron tick lists checks in batches instead of one KV round-trip per check row every minute (main.ts)
- [x] Page shell and JSON responses are gzipped when the client accepts it — 134KB shell drops to ~33KB (main.ts)
- [x] Dashboard boot fetches status/monitors/secrets in parallel instead of three sequential round-trips (main.ts loadDashboard)
- [x] Double-clicking wizard step 1 Save no longer creates two monitors — mutating buttons disable while in flight (main.ts)
- [x] Save secret, Delete secret, Delete monitor, Duplicate, Edit details and the webhook key buttons show a pending state during their request (main.ts)
- [x] Copy webhook secret flashes "Copied!" on success instead of doing nothing visible (main.ts)
- [x] Dashboard shows a loading placeholder instead of "No monitors yet" while monitors are still loading (main.ts)
- [x] Switching the Reports window no longer blanks the whole list to "Loading…" and hard-flashes the page (main.ts)
- [x] Run now refreshes the dashboard when it finishes and shows the real error text when it fails (main.ts)
- [x] The Reports 24h/7d/30d pill highlight matches the window actually shown after leaving Reports and returning (main.ts)
- [x] Keyboard focus is visible on every button, input, checkbox and tab instead of being erased by a blanket outline:none (main.ts)
- [x] Buttons give press feedback on :active instead of only changing once the network responds (main.ts)
- [x] A run's details can be opened from the keyboard, not only by mouse click (main.ts)
- [x] Modals close on Escape, trap focus, and return focus to the control that opened them (main.ts)
- [x] A session expiry mid-request closes any open modal instead of leaving it floating over the login screen (main.ts)
- [x] The wizard's Response path validation error names that field instead of the nonexistent "JSON expression" (main.ts)
- [x] Wizard steps 1 and 2 scroll their validation errors into view the way step 3 does (main.ts)
- [x] The comparator dropdown reads "is greater than" rather than "gt (>)" while still sending gt/lt/gte/lte/eq (main.ts)
- [x] The two hints using the undefined .help-text class render at hint size instead of full body text (main.ts)
